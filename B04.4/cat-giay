#include <bits/stdc++.h>
using namespace std;
int main () {
    long long m, n, k, s=0, a, b;
    cin >> m >> n;
    while (m > 0 && n > 0) {
        a = max(m, n);
        b = min(m, n);
        m=a;
        n=b;
        s+=m/n;
        m=m%n;
    }
    cout << s;
}
